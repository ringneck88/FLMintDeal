export default {
  register() {
    // Empty register function
  },

  bootstrap() {
    // Empty bootstrap function to prevent build issues
    console.log('Bootstrap skipped for build compatibility');
  },
};
