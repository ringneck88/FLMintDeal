{
  "name": "frontend",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages publish dist",
    "wrangler": "wrangler"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^5.13.0",
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "wrangler": "^4.32.0"
  }
}
